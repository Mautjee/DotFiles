local masonConfig = require("mauro.lsp.mason")
local lspConfig = require("mauro.lsp.lspconfig")
local cmpConfig = require("mauro.lsp.cmp")
local nulllsConfig = require("mauro.lsp.null-ls")

return {
    masonConfig,
    lspConfig,
    cmpConfig,
    nulllsConfig,
}
